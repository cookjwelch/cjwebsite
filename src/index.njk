---
layout: base.njk
title: Home
---

<section id="about" class="about-section">
  <h2>About Me</h2>
  
  <p class="section-label">//now//</p>
  <ul class="highlight-list">
    <li>MBA Candidate at <a href="https://www.bc.edu/bc-web/schools/carroll-school.html" target="_blank" rel="noopener">Boston College Carroll Graduate School of Management</a></li>
  </ul>
  
  <p class="section-label">//previously//</p>

  <div class="experience-entry">
    <p class="company-name">LogixHealth</p>
    <p class="job-title">Training Supervisor</p>
    <ul class="job-duties">
      <li>Analyzed, redesigned, and automated internal processes to streamline workflows and reduce turnaround time by an hour per day per specialist</li>
      <li>Supervised and trained new departmental hires, ensuring onboarding consistent with departmental standards</li>
      <li>Responsible for mentoring and retraining employees on performance improvement</li>
      <li>Maintained and updated comprehensive training documentation and workflow materials</li>
    </ul>

    <p class="job-title">Data Specialist</p>
    <ul class="job-duties">
      <li>Analyzed and reported on medical chart data in support of coding and billing operations</li>
      <li>Built and maintained dashboards for performance monitoring</li>
      <li>Managed daily chart flow across teams, handling thousands of accounts</li>
      <li>Collaborated cross-functionally to gather business requirements and define deliverables</li>
    </ul>
  </div>

  <div class="experience-entry">
    <p class="company-name">Physical Activity and Exercise Intervention Research Lab</p>
    <p class="job-title">Research Assistant</p>
    <ul class="job-duties">
      <li>Assisted exercise physiologists with daily operations while completing a research project</li>
      <li>Initiated data analysis to find significant trends to illustrate the pattern of losing weight by comparing the relationship of metabolic rate to weight loss results</li>
    </ul>
  </div>

  <p class="section-label">//skills//</p>
  <div class="skills-grid">
    <ul class="skills-column">
      <li>Excel</li>
      <li>Power BI</li>
      <li>Python</li>
    </ul>
    <ul class="skills-column">
      <li>R</li>
      <li>SQL</li>
      <li>Data Analysis</li>
    </ul>
    <ul class="skills-column">
      <li>Process Optimization</li>
      <li>Workflow Design</li>
      <li>Training</li>
    </ul>
  </div>

</section>

<section id="shelf" class="tabs-container">
  <nav class="tab-nav">
    <button class="tab-btn active" data-tab="bookshelf">Bookshelf</button>
    <button class="tab-btn" data-tab="projects">Projects</button>
  </nav>
  
  <!-- Bookshelf Tab -->
  <div id="bookshelf" class="tab-content active">
    
    <!-- Currently Reading -->
    {% if books.currentlyReading | length > 0 %}
    <div class="year-section">
      <h3 class="year-label">📖 Currently Reading</h3>
      <div class="book-grid">
        {% for book in books.currentlyReading %}
        <div class="book-card" data-title="{{ book.title }}" data-author="{{ book.author }}">
          {% if book.cover %}
          <img src="{{ book.cover }}" alt="{{ book.title }}" loading="lazy">
          {% else %}
          <div class="book-placeholder"><span>{{ book.title }}</span></div>
          {% endif %}
        </div>
        {% endfor %}
      </div>
    </div>
    {% endif %}

    <!-- Want to Read -->
    {% if books.wantToRead | length > 0 %}
    <div class="year-section">
      <h3 class="year-label">📚 Want to Read</h3>
      <div class="book-grid">
        {% for book in books.wantToRead %}
        <div class="book-card" data-title="{{ book.title }}" data-author="{{ book.author }}">
          {% if book.cover %}
          <img src="{{ book.cover }}" alt="{{ book.title }}" loading="lazy">
          {% else %}
          <div class="book-placeholder"><span>{{ book.title }}</span></div>
          {% endif %}
        </div>
        {% endfor %}
      </div>
    </div>
    {% endif %}

    <!-- Finished Books -->
    {% if books.finished | length > 0 %}
    <div class="year-section">
      <h3 class="year-label">✓ Finished ({{ books.finished | length }} books)</h3>
      <div class="book-grid">
        {% for book in books.finished %}
        <div class="book-card" data-title="{{ book.title }}" data-author="{{ book.author }}">
          {% if book.cover %}
          <img src="{{ book.cover }}" alt="{{ book.title }}" loading="lazy">
          {% else %}
          <div class="book-placeholder"><span>{{ book.title }}</span></div>
          {% endif %}
        </div>
        {% endfor %}
      </div>
    </div>
    {% endif %}
    
    {% if books.finished | length == 0 and books.currentlyReading | length == 0 and books.wantToRead | length == 0 %}
    <p class="no-books">No books yet. Add some books on <a href="https://literal.club/cookjwelch" target="_blank">Literal</a>!</p>
    {% endif %}
  </div>
  
  <!-- Projects Tab -->
  <div id="projects" class="tab-content">
    <ul class="projects-list">
      {% for project in projects.projects %}
      <li class="project-item">
        <a href="{{ project.url }}" class="project-title">{{ project.title }}</a>
        <p>{{ project.description }}</p>
        <span class="project-meta">{{ project.tech }} · {{ project.year }}</span>
      </li>
      {% endfor %}
    </ul>
  </div>
</section>
